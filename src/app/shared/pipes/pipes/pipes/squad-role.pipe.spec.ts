import { SquadRolePipe } from './squad-role.pipe';

describe('SquadRolePipe', () => {
  it('create an instance', () => {
    const pipe = new SquadRolePipe();
    expect(pipe).toBeTruthy();
  });
});
