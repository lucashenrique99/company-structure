import { ReleaseTrainRolePipe } from './release-train-role.pipe';

describe('ReleaseTrainRolePipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseTrainRolePipe();
    expect(pipe).toBeTruthy();
  });
});
